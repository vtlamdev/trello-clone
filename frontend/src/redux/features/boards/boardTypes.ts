/*
export interface Board {
    id: string;
    title: string;
    lists: List[];
}

export interface List {
    id: string
    title: string;
    cards: Card[];
}

export interface Card {
    id: string;
    title: string;
}
*/

// Other action types or constants